import React from "react";
import ReactDOM from "react-dom/client";
import AutoHub from "./AutoHub.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AutoHub />
  </React.StrictMode>
);
